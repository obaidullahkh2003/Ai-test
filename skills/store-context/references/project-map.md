# Project Map

- `backend/`: domain entities, services, REST controllers, security config, Liquibase migrations.
- `frontend/`: Angular app shell for products/cart/admin flow.
- `docker-compose.yml`: local stack with postgres + keycloak + backend + frontend.
- `helm/computer-parts-store`: Kubernetes deployment manifests.
