services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: storedb
      POSTGRES_USER: store
      POSTGRES_PASSWORD: store
    ports: ["5432:5432"]
  keycloak:
    image: quay.io/keycloak/keycloak:25.0
    command: start-dev
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports: ["8081:8080"]
  backend:
    build: ./backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/storedb
      SPRING_DATASOURCE_USERNAME: store
      SPRING_DATASOURCE_PASSWORD: store
      APP_SECURITY_ISSUER_URI: http://keycloak:8080/realms/computer-store
    ports: ["8080:8080"]
    depends_on: [postgres, keycloak]
  frontend:
    build: ./frontend
    ports: ["4200:80"]
    depends_on: [backend]
