apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: { name: parts-ingress }
spec:
  rules:
    - host: {{ .Values.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parts-frontend
                port: { number: 80 }
