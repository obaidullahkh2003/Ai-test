apiVersion: v1
kind: Service
metadata: { name: parts-backend }
spec:
  selector: { app: parts-backend }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: v1
kind: Service
metadata: { name: parts-frontend }
spec:
  selector: { app: parts-frontend }
  ports: [{ port: 80, targetPort: 80 }]
