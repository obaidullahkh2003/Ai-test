# Frontend (Angular)

The UI includes:
- Login entry point (delegated to Keycloak)
- Product listing
- Cart and checkout views
- Admin dashboard placeholders

For a full generated Angular workspace, run `npx @angular/cli@17 new` and copy `src/app` contents from this repository.
