# Angular Frontend

This folder is prepared for an Angular + Angular Material app with:
- Login view
- Product catalog
- Cart/Checkout
- Admin dashboard

Generate full app:
```bash
npx @angular/cli@latest new . --standalone --routing --style=scss
```
